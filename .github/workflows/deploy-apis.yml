name: Deploy Tyk APIs

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      ENV: ${{ github.event.inputs.environment }}

    steps:
      - uses: actions/checkout@v4

      ####################################################
      # Install summon
      ####################################################
      - name: Install summon
        run: |
          curl -L https://github.com/cyberark/summon/releases/latest/download/summon-linux-amd64.tar.gz | tar zx
          sudo mv summon /usr/local/bin/summon
          chmod +x /usr/local/bin/summon

      ####################################################
      # Install summon-conjur
      ####################################################
      - name: Install summon-conjur
        run: |
          sudo mkdir -p /usr/local/lib/summon
          curl -L https://github.com/cyberark/summon-conjur/releases/latest/download/summon-conjur-linux-amd64 \
            -o summon-conjur
          chmod +x summon-conjur
          sudo mv summon-conjur /usr/local/lib/summon/summon-conjur

      ####################################################
      # Verify summon providers
      ####################################################
      - name: Verify summon providers
        run: |
          ls -l /usr/local/lib/summon
          summon -V

      ####################################################
      # Install envsubst
      ####################################################
      - name: Install envsubst
        run: |
          sudo apt-get update
          sudo apt-get install -y gettext-base
      
      - name: Debug tools
        run: |
          which summon || true
          which summon-conjur || true
          which envsubst || true

      - name: Verify summon provider
        run: |
          which summon || true
          ls -l /usr/local/lib/summon || true
          summon --version || true

      - name: Debug rendering
        run: |
          set -ex
          ls -R
          which summon
          ls -l /usr/local/lib/summon || true
          which envsubst

      ####################################################
      # Render APIs using Conjur secrets
      ####################################################
      - name: Render API definitions
        env:
          CONJUR_APPLIANCE_URL: ${{ secrets.CONJUR_URL }}
          CONJUR_ACCOUNT: ${{ secrets.CONJUR_ACCOUNT }}
          CONJUR_AUTHN_LOGIN: admin
          CONJUR_AUTHN_API_KEY: ${{ secrets.CONJUR_API_KEY }}
        run: |
          set -ex
          mkdir -p rendered-apis
          ls -R

          for file in $ENV/apis/*.json
          do
            API=$(basename $file .json)

            summon --provider summon-conjur
                   -f secrets/conjur-map.yml \
                   -D ENV=$ENV \
                   -D API=$API \
                   bash -c "/usr/bin/envsubst < '$file' > rendered-apis/$API.json"
          done

      ####################################################
      # Generate tyk-sync index file
      ####################################################
      - name: Generate .tyk.json
        run: |
          cd rendered-apis
          echo '{"type":"apis","files":[' > .tyk.json

          first=true
          for file in *.json
          do
            if [ "$first" = true ]; then
              echo "\"$file\"" >> .tyk.json
              first=false
            else
              echo ",\"$file\"" >> .tyk.json
            fi
          done

          echo "]}" >> .tyk.json
          cat .tyk.json

      ############################################
      # Deploy APIs using tyk-sync Docker image
      ############################################
      - name: Sync APIs to Tyk
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/rendered-apis:/app/data \
            tykio/tyk-sync:v2.1.6 \
            sync \
            --dashboard "${{ secrets.TYK_DASHBOARD_URL }}" \
            --secret "${{ secrets.TYK_DASHBOARD_API_KEY }}" \
            --path /app/data \
            --no-delete